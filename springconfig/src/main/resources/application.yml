server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/manikmar/config-server
          clone-on-start: true


management:
  endpoints:
    web:
      exposure:
        include: "*"

  security:
    enabled: false

